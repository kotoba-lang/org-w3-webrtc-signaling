{:paths ["src" "test"]
 :tasks
 {test
  {:task (do (require 'clojure.test 'kotoba.rt.core-test)
             (let [r (clojure.test/run-tests 'kotoba.rt.core-test)]
               (when (pos? (+ (:fail r) (:error r))) (System/exit 1))))}}}
